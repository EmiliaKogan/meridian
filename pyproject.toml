[project]
name = "meridian"
version = "0.1.0"
description = "Meridian bike-share data platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.19.0",
    "httpx>=0.28.1",
    "psycopg>=3.3.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
